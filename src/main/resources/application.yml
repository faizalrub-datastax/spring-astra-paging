spring:
  data:
    cassandra:
      keyspace-name: content
      username: <>
      Password: <>
      schema-action: create-if-not-exists
      request:
        timeout: 10s
      connection:
        connect-timeout: 10s
        init-query-timeout: 10s
datastax.astra:
  secure-connect-bundle: secure-connect-test.zip
astra.db:
  id: <>
  region: us-east-1
  keyspace: main
  application.token: <>